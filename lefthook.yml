# Use this to build commit messages
prepare-commit-msg:
  scripts:
    "prepare-commit-msg":
      runner: bash

# Use this to validate commit messages
commit-msg:
  commands:
    "lint commit message":
      run: npx commitlint --edit
